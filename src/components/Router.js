import { BrowserRouter as Router, Route, Link as LinkRouter,Redirect } from "react-router-dom";
import styled from "styled-components";
const Link = styled(LinkRouter)`
  color : inherit;
  text-decoration : none;
`;
export {Link,Router,Route,Redirect};
